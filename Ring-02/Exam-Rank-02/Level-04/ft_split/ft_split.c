#include <stdlib.h>

char    **ft_split(char *str)
{
	int i;
	int j;
	char **result;
	
	result = malloc(); // not sure. 
	if (!result)
		return NULL; // not sure.

	i = 0;
	j = 0;
	while (str[0][j] != '\0')
	{

	j++;
	}
}
